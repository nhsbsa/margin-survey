import React from "react";import TaskItem from "./TaskItem";const TaskList=()=>{const tasks=[{title:"Check patient details",status:"Incomplete"},{title:"Review medical history",status:"In progress"},{title:"Confirm next appointment",status:"Complete"}];return(<ul className="nhsuk-task-list">{tasks.map((task,index)=>(<TaskItem key={index} title={task.title} status={task.status}/>))}</ul>);};export default TaskList;
